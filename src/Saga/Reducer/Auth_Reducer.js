import * as ActionType from '../ActionTypes';


const initVal = {
    isloading: false,
    user : null,
    error : ''
}


export const authReducer = (state = initVal , action) => {
    switch (action.type) {
        default :
            return state
    }
}